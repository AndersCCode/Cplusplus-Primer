#include <iostream>

int main() {
    std::cout << "Enter two numbers:" << std::endl;

    int v1 = 0, v2 = 0;

    std::cout << "The sum of " << v1;
              << "and " << v2;                      // Add std::cout in the beginning
              << " is " << v1 * v2 << std::endl;    // Add std::cout in the beginning

    return 0;
}